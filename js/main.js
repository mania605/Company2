import Anime from "./anime.js";

const h1 = document.querySelector("header h1");

const scroll_btns = ducument.querySelectorAll('.scroller li');

scroll_btns.forEach(btn => {
  btns.addEventListener("click", e => {
  });

